-- | Exercises for chapter 4.

module Chapter03 (
  safetail, safetail', safetail'', or', or'', or''', and', and''
  ) where

-- * Exercise 1

-- | TODO: define safetail using a conditional expression
safetail :: [a] -> [a]
safetail xs = undefined

-- | TODO: define safetail using guarded equations
safetail' :: [a] -> [a]
safetail' xs = undefined

-- | TODO: define safetail using pattern matching
safetail'' :: [a] -> [a]
safetail'' xs = undefined

-- * Exercise 2

-- | TODO: define the logical or operator (||) using pattern matching (1)
or' :: Bool -> Bool -> Bool
or' a b = undefined

-- | TODO: define the logical or operator (||) using pattern matching (2)
or'' :: Bool -> Bool -> Bool
or'' a b = undefined

-- | TODO: define the logical or operator (||) using pattern matching (3)
or''' :: Bool -> Bool -> Bool
or''' a b = undefined

-- * Exercise 3

-- | TODO: define the logical and operator (&&) using a conditional expression
and' :: Bool -> Bool -> Bool
and' a b = undefined

-- * Exercise 4

-- | TODO: define an alternative logical and operator (&&) using a conditional expression
and'' :: Bool -> Bool -> Bool
and'' a b = undefined